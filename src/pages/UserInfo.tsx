import React from "react";
import BottomNavbar from "../components/BottomNavbar";
const UserInfo = () => {
  return (
    <div>
      UserInfo
      <BottomNavbar />
    </div>
  );
};

export default UserInfo;
